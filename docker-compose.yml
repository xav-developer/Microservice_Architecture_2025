services:

    authorization:
        build:
            context: application/authorization
            dockerfile: Dockerfile
            target: compose
            args:
                UID: ${UID}
                GID: ${GID}
        volumes:
            - ./application/authorization/html:/var/www/html/:rw
        ports:
            -   target: 8000
                published: 8001
        depends_on:
            - postgres
        networks:
            private:

    profile:
        build:
            context: application/profile
            dockerfile: Dockerfile
            target: compose
            args:
                UID: ${UID}
                GID: ${GID}
        volumes:
            - ./application/profile/html:/var/www/html/:rw
        ports:
            -   target: 8000
                published: 8002
        depends_on:
            - postgres
        networks:
            private:

    postgres:
        image: postgres:17.7-trixie
        environment:
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
            POSTGRES_DB: ${POSTGRES_DB}
        volumes:
            - postgres:/var/lib/postgresql/data:rw
        networks:
            private:

volumes:
    postgres:
        name: otus_msa_2025_postgres

networks:
    private:
        name: otus-msa-2025
        driver: bridge
